# =====================================================================
# Secrets & credentials — NEVER commit these
# =====================================================================
.env
.env.*
!.env.example
# MSAL refresh-token cache (contains a live refresh token)
.pbi_token_cache.json
*.pbi_token_cache.json

# =====================================================================
# Real configuration — contains tenant/workspace/dataset GUIDs and
# internal entity codes. Copy the *.example.* files and fill in your own.
# =====================================================================
powerbi-summary-agent/config/config.json
powerbi-summary-agent/config/business_rules.md

# =====================================================================
# Generated run artifacts — contain real business data, model schema,
# and financial numbers. Regenerated by a live run.
# =====================================================================
outputs/
outputs_replay/
outputs_bizlang_preview/
powerbi-summary-agent/outputs/
powerbi-summary-agent/outputs_replay/
powerbi-summary-agent/outputs_bizlang_preview/

# =====================================================================
# Python
# =====================================================================
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.ruff_cache/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# Virtual environments
venv/
.venv/
env/
ENV/

# =====================================================================
# OS / editor noise
# =====================================================================
.DS_Store
Thumbs.db
desktop.ini
.idea/
*.swp
