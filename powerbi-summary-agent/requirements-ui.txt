# Extra dependencies for the client-onboarding UI only.
#
# Deliberately separate from requirements.txt: the scheduled agent job must not
# grow a web framework it never imports, and the UI image installs both.
#
#     pip install -r requirements.txt -r requirements-ui.txt
#     python -m src.api.app

fastapi>=0.115
uvicorn[standard]>=0.30
