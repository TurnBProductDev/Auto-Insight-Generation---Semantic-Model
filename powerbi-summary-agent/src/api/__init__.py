"""The client-onboarding API and its single-page wizard.

Runs in the agent's own environment so ``src`` imports work directly and the
probe cannot drift from the pipeline it is meant to predict.
"""
